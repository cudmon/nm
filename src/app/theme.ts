"use client";

import { createTheme } from "@mantine/core";

export const theme = createTheme({
  defaultRadius: "xs",
  primaryColor: "blue",
  fontFamily: "Jost, sans-serif",
  headings: {
    fontFamily: "Jost, sans-serif",
  },
});
